import heapq

# Function to read graph from file
def read_graph(filename):
    graph = {}
    with open(filename, 'r') as f:
        for line in f:
            city1, city2, dist = line.strip().split()
            dist = int(dist)

            if city1 not in graph:
                graph[city1] = {}
            if city2 not in graph:
                graph[city2] = {}

            graph[city1][city2] = dist
            graph[city2][city1] = dist  # undirected graph

    return graph


# Dijkstra Algorithm
def dijkstra(graph, start):
    pq = [(0, start)]
    distances = {node: float('inf') for node in graph}
    distances[start] = 0

    while pq:
        cost, current = heapq.heappop(pq)

        for neighbor, weight in graph[current].items():
            new_cost = cost + weight
            if new_cost < distances[neighbor]:
                distances[neighbor] = new_cost
                heapq.heappush(pq, (new_cost, neighbor))

    return distances


# Main
if __name__ == "__main__":
    graph = read_graph("cities.txt")
    start = input("Enter start city: ")

    if start not in graph:
        print("City not found!")
    else:
        result = dijkstra(graph, start)

        print("\nShortest distances from", start)
        for city in result:
            print(city, ":", result[city])print("Shortest distances from", start_city)

for city, distance in result.items():
    print(city, ":", distance, "km")
